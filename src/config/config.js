/**
 * @deprecated
 * Toda la configuración fue centralizada en `src/config/index.js`.
 * Mantener este archivo evita romper imports legacy durante la transición.
 */
import config from './index.js';

export default config;
